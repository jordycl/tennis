{% extends 'base.html.twig' %}

{% block title %}Toernooi{% endblock %}

{% block body %}
    <h1>Toernooi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ toernooi.id }}</td>
            </tr>
            <tr>
                <th>Omschrijving</th>
                <td>{{ toernooi.omschrijving }}</td>
            </tr>
            <tr>
                <th>Datum</th>
                <td>{{ toernooi.datum ? toernooi.datum|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('toernooi_index') }}">back to list</a>

    <a href="{{ path('toernooi_edit', {'id': toernooi.id}) }}">edit</a>

    {{ include('toernooi/_delete_form.html.twig') }}
{% endblock %}
